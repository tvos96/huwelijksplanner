[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Single-page app: alle niet-function routes naar index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
